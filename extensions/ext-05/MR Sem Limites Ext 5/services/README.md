# services/

Camada de serviços de negócio (licença, upload, prompt, telemetria).
Cada serviço consome o **adapter** em `../adapters/` e nunca chama o backend
diretamente. FASE 2A: pasta reservada, sem implementação.
